import { DashReplacerPipe } from './dash-replacer.pipe';

describe('DashReplacerPipe', () => {
  it('create an instance', () => {
    const pipe = new DashReplacerPipe();
    expect(pipe).toBeTruthy();
  });
});
